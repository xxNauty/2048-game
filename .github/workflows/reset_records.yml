name: Reset records on every pull request.

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

permissions:
  contents: write

jobs:
  reset-records:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout PR branch
      uses: actions/checkout@v3
      with:
        ref: ${{ github.event.pull_request.head.ref }}
        repository: ${{ github.event.pull_request.head.repo.full_name }}

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'

    - name: Install requirements (if any)
      run: pip install -r requirements.txt || true

    - name: Run reset_records.py
      run: python scripts/reset_records.py

    - name: Commit and push changes
      run: |
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git config --global user.name "records-manager[bot]"
        git add .
        git commit -m "Records reset" || echo "No changes to commit"
        git push origin HEAD:${{ github.event.pull_request.head.ref }}